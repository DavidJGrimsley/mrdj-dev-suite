---
"@mr.dj2u/cli": patch
"@mr.dj2u/knowledge": patch
---

Keep initialized workspace control repositories limited to canonical project memory and `mds.workspace.json`. Retrospective evidence and handoff guidance now live in temporary review packets, while workspace status derives live worktrees from Git instead of a persisted worktree registry.
