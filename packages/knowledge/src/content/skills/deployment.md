# Skill: Deployment Readiness

Use before shipping an Expo app.

Run lint, typecheck, tests, Expo Doctor, and the production build profile
locally when available. Check env docs, API route output mode, metadata, and
rollback notes.

