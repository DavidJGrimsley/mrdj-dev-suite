# Skill: API Routes

Use when building Expo Router API routes.

Prefer explicit method handling, request validation, typed response envelopes,
auth checks before privileged work, and no direct service-role usage without an
authorization boundary.

