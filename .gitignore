node_modules/
dist/
coverage/
.turbo/
.cache/
.expo/
*.tsbuildinfo

.env
.env.*
!.env.example

temp/
# test-apps folder is purposfully not ignored, as we sometimes work on a generated app there and then apply the changes to the generator and we want to be ale to push wip commits for the generated app.
.pnpm-store/

# Build-generated plugin skill files (sourced from packages/knowledge at build time)
plugins/claude-code/skills/

.claude/worktrees/

*.code-workspace

# Local nested-repo backup folders used when flattening embedded git repos
**/.git.backup-*/

test-apps/